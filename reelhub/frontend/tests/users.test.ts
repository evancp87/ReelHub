import { it } from "node:test"
import axios from 'axios';
import { useCreateUserMutation, useLoginUserMutation } from "../src/store/services/userApi";

jest.mock('axios');

function userAuth() {
    describe("media", () => {
        it("Should log the user in and return a token", () => {
        }),
        
        it("Should register the user and return a token", () => {
        })
        ;
    });
}
